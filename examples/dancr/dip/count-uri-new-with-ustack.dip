# Who creates URI objects and how many?
# Only tell me when they are created by Dancer code,
# that is, not by the tests themselves.

before { count('constructor', ustack(5)) }
    call 'URI::new' & cflow qr/Dancer/;
